// همخونه - نسخه اولیه

function showMessage(text) {
    alert(text);
}

function login() {
    showMessage("ورود با موفقیت انجام شد (نسخه آزمایشی)");
}

function register() {
    showMessage("ثبت‌نام با موفقیت انجام شد (نسخه آزمایشی)");
}

function createRoommateAd() {
    showMessage("آگهی همخانه ثبت شد (نسخه آزمایشی)");
}

function createHouseAd() {
    showMessage("آگهی خانه ثبت شد (نسخه آزمایشی)");
}

function searchAds() {
    showMessage("جستجو انجام شد (نسخه آزمایشی)");
}

function sendMessage() {
    showMessage("پیام ارسال شد (نسخه آزمایشی)");
}
